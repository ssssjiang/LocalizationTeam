# Monet四目+Odo标定方案

# 方案一：

类似之前左目到odo标定方案，分别进行左目到odo、左侧目到odo、右侧目到odo标定。

实现：主函数循环3次标定：

* 读json文件和对应相机图片

* slam建图

* 建图完成GBA后slam定位

* 相机到odo时间同步

* 相机到odo标定

* 判断标定误差是否满足阈值

* 标定结果写入json文件

优点：1、在原有框架下实现方便，更快完成；

2、相机是分别标定到odo，不涉及相机之间的变换误差和时间延迟误差。

缺点：耗时增加，原标定算法耗时的3倍，日志量是原来的3倍。





