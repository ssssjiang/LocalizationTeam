# 滑轨检测算法逻辑

# 1. 平面度

## 1.1 棋盘格角点检测：

1. 其中img为原图，KK， dd为内参，去畸变后投影矩阵不变

2. 得到：org\_pts为去畸变前的点，undis\_pts为去畸变后的图像点，norn\_ptr为归一化项平面点

```c++
   int board_size_x = 9; // 设置棋盘格的宽度（列数）
   int board_size_y = 9; // 设置棋盘格的高度（行数）
   cv::Size board_size = cv::Size(board_size_x, board_size_y);  
   
bool found = cv::findChessboardCorners(img, board_size, corners,cv::CALIB_CB_ADAPTIVE_THRESH | cv::CALIB_CB_NORMALIZE_IMAGE);
   cv::Mat ru_gray;
   cv::cvtColor(img, ru_gray, cv::COLOR_BGR2GRAY); // 转换为灰度图
   //cv::Size win(5,5);
   cv::Size win(15,15);
   cv::Size zeroZone(-1,-1);
   cv::TermCriteria cri(cv::TermCriteria::EPS + cv::TermCriteria::COUNT,30,0.01);
   cv::cornerSubPix(ru_gray,corners,win,zeroZone,cri);
    corners = reorder (corners);
    
    
cv::undistortPoints(corners,nice_corners,KK,dd,KK);
       org_pts  = corners;
       undis_pts = nice_corners;

Point2d pixel2cam(const Point2d &p, const Mat &K) {
//   double dx=(p.x - K.at<double>(0, 2)) / K.at<double>(0, 0);
//   double dy=(p.y - K.at<double>(1, 2)) / K.at<double>(1, 1);
//   cout<<p.x<<" "<< K.at<float>(0, 2)<< endl;
// //   cout<<dx<< "  " <<dy<<endl;
//  cout<<K<<endl;
  return Point2d(
    (p.x - K.at<float>(0, 2)) / K.at<float>(0, 0),
    (p.y - K.at<float>(1, 2)) / K.at<float>(1, 1)
  );
}

norn_pts.push_back(  pixel2cam(undis_pts[i],KK));
```

## 1.2 平面度检测

```plain&#x20;text
# 三角化  
left_cam.norn_pts为归一化像平面点
cv::triangulatePoints(单位阵,右目相对左目外参,cv::Mat(left_cam.norn_pts),cv::Mat(right_cam.norn_pts),pts_4d);


# 求解平面
   vector< Matrix<double ,4,1 > >  eg_pts;
   for (int i = 0; i < pts_4d.cols; i++) 
   {
      Mat x = pts_4d.col(i);
      x /= x.at<float>(3, 0); // 归一化
      Point3d p(
        x.at<float>(0, 0),
        x.at<float>(1, 0),
        x.at<float>(2, 0)
      );
     out_file << x.at<float>(0, 0)<<" "<<x.at<float>(1, 0)<<" "<<x.at<float>(2, 0)<<endl;
      Eigen::Matrix<double ,4,1 > tmp_p;
      tmp_p<<x.at<float>(0, 0),x.at<float>(1, 0),x.at<float>(2, 0),1;
      eg_pts.push_back(tmp_p);

      ave_x=ave_x + x.at<float>(0, 0) ;
      ave_y=ave_y + x.at<float>(1, 0) ;
      ave_z=ave_z + x.at<float>(2, 0) ;
   }
    
   ave_x=ave_x/pts_4d.cols; // 三角化后的棋盘格点中心点
   ave_y=ave_y/pts_4d.cols;
   ave_z=ave_z/pts_4d.cols;

   int N= eg_pts.size();
   Eigen::MatrixXd A(N,4);
   for(int i=0;i<eg_pts.size();i++ )
   {
      A(i,0)= eg_pts[i].x();
      A(i,1)= eg_pts[i].y();
      A(i,2)= eg_pts[i].z();
      A(i,3)= 1;

   } 
   Eigen::JacobiSVD<Eigen::MatrixXd> svd(A,Eigen::ComputeThinV);
   Eigen::MatrixXd abdc = svd.matrixV().col(3);
   plane_para =  abdc ;
   cout<<"$$$4 " <<endl;
   cout<<A<<endl;
   cout<<abdc<<endl;

   ofstream tmp_pingmian_canshu;
   tmp_pingmian_canshu.open(self_path+"/pingmian_canshu.txt",ios::out);
   tmp_pingmian_canshu<<"平面参数 "<<abdc.transpose()<<endl;
   
   # 求解指标
  double max_dis=-10000;
   double pingmian_dis_sum=0;
  for(auto m:eg_pts)
  {
     auto d=abdc.transpose()*m;
     double fenmu = sqrt(abdc(0)* abdc(0)+ abdc(1)* abdc(1)+abdc(2)* abdc(2) );
     double dis=std::abs(d(0))/fenmu;
     if(dis>max_dis)
     {
        max_dis=dis;
     }
     //cout<<"dis"<<endl;
    // cout<<dis<<endl;
     pingmian_dis_sum=pingmian_dis_sum+dis;
  }

  double ave_pingmian_dis=pingmian_dis_sum/eg_pts.size();
  ave_pingmiandu= ave_pingmian_dis; // 点到平面距离均值
  max_pingmiandu=max_dis;// 点到平面距离最大值

```

# 2. 行差检测（使用StereoRectify后的图片提棋盘格点，与其他检测不一样）

```c++
cv::Mat  R1,R2,P1,P2,Q;
  cv::Size imagesize = cv::Size(1280,1088);


  LOG(INFO) << "calculate undis info";
  auto left_undis_K = cv::getOptimalNewCameraMatrix(
      g_stere.cv_left_in, g_stere.cv_left_dis, imagesize, 0);
  // 因为模型问题，可能直接StereoRectify会导致图像异常，所以算法先对左右目去畸变，再StereoRectify
  // 1. 图像去畸变
  cv::Mat left_undis_x, left_undis_y;
  cv::initUndistortRectifyMap(g_stere.cv_left_in, g_stere.cv_left_dis, {},
                              left_undis_K, imagesize, CV_32FC1, left_undis_x,
                              left_undis_y);

  auto right_undis_K = cv::getOptimalNewCameraMatrix(
      g_stere.cv_right_in, g_stere.cv_right_dis, imagesize, 0);

  cv::Mat right_undis_x, right_undis_y;
  cv::initUndistortRectifyMap(g_stere.cv_right_in, g_stere.cv_right_dis, {},
                              right_undis_K, imagesize, CV_32FC1, right_undis_x,
                              right_undis_y);

  LOG(INFO) << "calculate stereorectify info " << left_undis_K.rows << ", "
            << left_undis_K.cols;

  // 2. 去畸变后stereoRectify
  cv::stereoRectify(left_undis_K, {}, right_undis_K, {}, imagesize,
                    g_stere.only_R, g_stere.only_t, R1, R2, P1, P2, Q,
                    cv::CALIB_ZERO_DISPARITY);
  ruyilog << "R1" << endl;
  ruyilog << R1 << endl;
  ruyilog << "R2" << endl;
  ruyilog << R2 << endl;
  ruyilog << "p1" << endl;
  ruyilog << P1 << endl;
  ruyilog << "P2" << endl;
  ruyilog << P2 << endl;
  ruyilog << "Q" << endl;
  ruyilog << Q << endl;

  //ruyilog<< "g_stere.only_t"  <<endl;
  //ruyilog<< g_stere.only_t  <<endl;
 

  cv::Mat undistmap1l,undistmap2l,undistmap1r,undistmap2r;//左相机输出1/2映射，右相机输出1/2映射

  cv::initUndistortRectifyMap(left_undis_K,{}, R1, P1, imagesize , CV_32FC1, undistmap1l, undistmap2l);
  cv::initUndistortRectifyMap(right_undis_K,{}, R2, P2, imagesize, CV_32FC1, undistmap1r, undistmap2r);

  LOG(INFO) << "do remap: undis " << left_cam.img.rows << ", " << left_cam.img.cols;
  cv::Mat undst_l,undst_r;
  cv::Mat dst_l,dst_r;
  cv::remap(left_cam.img,undst_l,left_undis_x,left_undis_y,cv::INTER_LINEAR);
  cv::remap(right_cam.img,undst_r,right_undis_x,right_undis_y,cv::INTER_LINEAR);

  LOG(INFO) << "do remap: stereoRectify";
  cv::remap(undst_l,dst_l,undistmap1l,undistmap2l,cv::INTER_LINEAR);
  cv::remap(undst_r,dst_r,undistmap1r,undistmap2r,cv::INTER_LINEAR);
  cv::imwrite(self_path+"/dst_l.png",dst_l);
  cv::imwrite(self_path+"/dst_r.png",dst_r);
  std::vector<cv::Point2f> l_pts =get81pts(dst_l,1);
  std::vector<cv::Point2f> r_pts =get81pts(dst_r,0);
  hangcha_sum=0;
  vector<cv::DMatch> hangcha_match;

  for(int i=0;i<81;i++)
  {
    double dx=l_pts[i].x-r_pts[i].x;
    double dy=l_pts[i].y-r_pts[i].y;
    // cout<<"dx"<<dx<<endl;
    cout<<"dy"<<dy<<endl;
    hangcha_sum=hangcha_sum+std::abs(dy);
    ruyilog<< "++++  "<< i <<endl;
    ruyilog<< "dy"  <<endl;
    ruyilog<< dy <<endl;

    ruyilog<< "l_pts[i].x"  <<endl;
    ruyilog<< l_pts[i].x <<endl;

    ruyilog<< "r_pts[i].x"  <<endl;
    ruyilog<< r_pts[i].x <<endl;

    ruyilog<< "l_pts[i].y"  <<endl;
    ruyilog<< l_pts[i].y <<endl;

    ruyilog<< "r_pts[i].y"  <<endl;
    ruyilog<< r_pts[i].y <<endl;

    ruyilog<< "end  "<<endl;
    DMatch tmpDmatch;
    tmpDmatch.queryIdx=i;
    tmpDmatch.trainIdx=i;
    hangcha_match.push_back(tmpDmatch);
  }

  hangcha_sum =  hangcha_sum/81;
```

# 3. 重投影误差检测

1. 先算PNP再重投影，计算误差

```c++
  vector<Point3d >   real_pts_3d;
  cv::Mat r,t;
  /// 根据棋盘格实际情况调整3d点位置
  for(int i=0;i<9  ;i++)
  {
    for(int j=0;j<9  ;j++)
    {
      double x=-0.5+0.1*j;
      double y=0.5-0.1*i;
      double z=3;
     Point3d p(
        x,
        y,
        z
      );
     real_pts_3d.push_back(p);
    }

  }

  solvePnP(real_pts_3d, left_cam.undis_pts, g_stere.cv_left_in, Mat(), r, t, false); 
  
  std::vector<cv::Point2d>  re_pts;
  cv::projectPoints(real_pts_3d,r,t,g_stere.cv_left_in, stere.cv_left_dis ,re_pts);
  double ave_pt=0;
  for(int i=0;i<81;i++)
  {
    // cout<<" chong tou yin wucha "<<endl;
    // cout<<" +++++++++++++ "<<endl;
    // cout<<re_pts[i].x << " "<<re_pts[i].y <<endl;
   //  cout<<right_cam.undis_pts[i].x << " "<<right_cam.undis_pts[i].y <<endl;
     double dx = re_pts[i].x - left_cam.undis_pts[i].x;
     double dy = re_pts[i].y - left_cam.undis_pts[i].y ;
     ave_pt = ave_pt+ sqrt(dx*dx+dy*dy);
    // cout<< "dx "<<dx<<endl;
    // cout<< "dy "<<dy<<endl;
  }
  ave_pt=ave_pt/re_pts.size();
  cout<<"left ave_pt " << ave_pt<<endl;
  re_err_left=ave_pt;
  ////////////////////////////////////
  //////// right  ///////////////// 
 
  solvePnP(real_pts_3d, right_cam.undis_pts, g_stere.cv_right_in, Mat(), r, t, false);  
  std::vector<cv::Point2d>  right_re_pts;
  cv::projectPoints(real_pts_3d,r,t,g_stere.cv_right_in, stere.cv_right_dis ,right_re_pts);
  
  double right_ave_pt=0;
  for(int i=0;i<81;i++)
  {
     cout<<"right chong tou yin wucha "<<endl;
     cout<<" +++++++++++++ "<<endl;
     cout<<right_re_pts[i].x << " "<<right_re_pts[i].y <<endl;
     cout<<right_cam.undis_pts[i].x << " "<<right_cam.undis_pts[i].y <<endl;
     double dx = right_re_pts[i].x - right_cam.undis_pts[i].x;
     double dy = right_re_pts[i].y - right_cam.undis_pts[i].y ;
     right_ave_pt = right_ave_pt+ sqrt(dx*dx+dy*dy);
     cout<< "dx "<<dx<<endl;
     cout<< "dy "<<dy<<endl;
  }
  right_ave_pt=right_ave_pt/re_pts.size();
  cout<<"right_ave_pt " << right_ave_pt<<endl;
  re_err_right = right_ave_pt;

```

# 4. 平面之间距离

```java
void RR_shuangmu::computer_diff()
{ 

   for(int i=1;i< v_rr_solo.size();i++)
   {
      double para_A = v_rr_solo[i].plane_para(0);
      double para_B = v_rr_solo[i].plane_para(1);
      double para_C = v_rr_solo[i].plane_para(2);
      double para_D = v_rr_solo[i].plane_para(3);
      double fenzi = para_A*v_rr_solo[i-1].ave_x + para_B*v_rr_solo[i-1].ave_y +para_C*v_rr_solo[i-1].ave_z  +para_D ;
      double fenmu=sqrt(  (para_A*para_A+ para_B *para_B +para_C*para_C ) );
      double tmp_diff= std::abs( fenzi  ) /fenmu ;
      v_rr_diff.push_back(tmp_diff);
      if(tmp_diff >g_para_calib.diff_err  )
      {
        isok= 9;
      }
   }
 
}
```
