# 打滑脱困日志整理

备注：数据目录中至少要包含RRLDR\_fprintf.log、SLAM\_fprintf.log、sensor.yaml三个文件

流程：

1. 下载对应日志，转换后rerun绘制轨迹图

2. 根据bug反馈时间，确定是否包含卡困时间，若包含，给出卡困前后的时间戳（AP时间），若不包含，则下载附近的其他包。(先根据UTC时间从slam\_normal.log中确定大概的AP时间戳，然后观察对应rerun轨迹前后是否困住)

3. 拷贝sensor.yaml到转换数据目录，拷贝整包数据到共享文件夹中并填写文档

