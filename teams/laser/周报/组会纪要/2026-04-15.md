# 多线组会纪要 · 2026-04-15

- **时间**：2026-04-15（周三）17:02–18:21（GMT+08）
- **主题**：04-15 | 多线组会
- **飞书智能纪要**：[04-15 | 多线组会 2026年4月15日](https://roborock.feishu.cn/docx/MgL4dhH5dowvdhxu6yUcdmiEnkf)

## 进展摘要（按主题）

- **耗时 / 多线程**：循环内多线程替换路径，单帧约 620 ms（与点云量相关）；需补线程占用与 top 日志。
- **面阵雷达**：明坤初步适配，后续闫冬（去畸变、点云、短帧时、初始化中位对齐、参数）。
- **8 m / 15 m**：与 15 m 全程对不齐、场地偏理想、噪点大；需极端测试场；60 场景相对可控。
- **SDK / 姚远**：log 参数不改、仅在线数据；周士伟侧联调推进；注意与 core 合入节奏避免撞节点。
- **ETCD**：优先过滤 0 距离、强度 0（飞点）；与姚远对齐；墙面强度 0 会误删部分点，评估可接受。
- **断流 2.0（王亚萌）**：ACC 先预留 demo，主线调框架；自测通过；择时 Rebase；**导航联调节点对齐为 4/21（周一）**。
- **丢弃过时雷达**：主体完成，待阈值后打开；鹏飞侧极端耗时 bug 优先级低。
- **Flora / 新图**：IMU 加速度异常 → 换 088；时间戳异常、外参抖动；严冬侧外参日志。
- **互换 / 误匹配 / BBS**：双重 kNN + 双阈值；大漂移集上 BBS 优于 ICP；需复查回环帧数、初值与搜索范围；测试模块验证阶段。
- **孙昂熙**：param 统一、日志解析修复、BBS3D best_score 初始化修正（从 0 起）、局部地图扩展验证等。
- **周士伟**：边缘 3D、特征失败并行 BBS3D、数据结构统一、重定位阈值与对称场景风险（下周）。
- **硬件**：MK2 协调标定/寄送（会议口径次日上午可寄）。

## 会议结论（要点）

1. **ETCD 过滤**为当前最急项之一，算法侧须落实。
2. **develop / loop**：不可用「loop 不合格就不评」替代可跑通、可对比的验证结论。
3. **雷达 log 格式统一**已合入。
4. **导航联调**节点：**4/21（周一）**。
5. **断流仍允许遥控**且长断流场景下重定位失败 → **提 bug**，追溯是否某次合入屏蔽防护。
6. **雷达快拆外参**：会议结论不紧迫，可与雪东沟通不必卡周五。
7. **互换**：需 **单一主 R**，王亚萌忙完后辅助框架。
8. **BBS3D best_score** 初始化问题修复后成功率与耗时表现改善。
9. **局部地图扩展**验证可行，下一步量化内存与耗时。
10. **多线程 / 并行 BBS**：需上机验证 CPU、断流，控制总线程并预留核心。

## 跟进 / 派发（从宋姝 daily 同步，便于组内检索）

- 跟进周士伟 + 姚远：**SDK 在线数据改动**联调收尾（当日/次日发测节奏），避免与 core 在节点前硬撞合入。
- 跟进明坤线 + 姚远：**ETCD — 过滤 0 距离点、强度 0（飞点）**；算法侧需落实；墙面强度 0 误删已评估可接受。
- 跟进闫冬：**面阵雷达**后续（去畸变、点云、短帧时、初始化中位对齐、参数）；**极端测试场**搭建以替换过理想场地（8 m/15 m 对不齐结论不外推）。
- 跟进闫冬：**扩展地图场景** — 断流约 70 s 仍允许遥控行走导致局部重定位失败；**断流禁遥控逻辑未生效** — **提 bug**，追溯是否某次合入（如王鑫相关）屏蔽防护。
- 跟进周士伟：与雪东沟通 **雷达快拆外参** 节点不必卡周五（会议结论：不紧迫；当前仍用单位阵风险可控）。
- 跟进闫冬（SDK/数据结构）：**点位数据结构/模板** 合并方案；**雷达外参消息扩展字段** 承载 Versa/Lumos 内参等 — 打包同步动态方；develop/loop 数据评估口径与明坤对齐（**不可用「loop 不合格就不评」替代可跑通验证**）。
- 跟进明坤侧工程：多线程耗时优化后 **补线程占用/top 日志**；**日志合并**（耗时打印、流程打印）。
- 跟进王亚萌：**丢弃过时雷达** — 阈值敲定后打开 drop；**Flora** — IMU 换 088、时间戳异常、外参抖动与严冬日志联动排查。
- 跟进王亚萌 + 互换链路主责：**互换/误匹配** — 会议明确需 **单一主 R**，亚萌忙完后辅助看框架。
- 跟进互换/BBS 验证 + 周士伟：**BBS/双重 kNN 阈值** — 测试模块封装合入路径；复查大漂移数据仅 1 个回环帧问题；试 **BBS 初值 + 缩小搜索**、地面/植被预处理。
- 跟进孙昂熙：**param 体系统一**上机验证与合入；**car characters + 在线 BBS** 上机；**局部地图扩展** 内存/耗时量化（执行前后 memory、vector 扩容余量）。
- 跟进周士伟：**边缘 3D** 与昂熙侧合并及 BBS3D 引入路径；**特征失败并行 BBS3D** 上机看内存/断流；**重定位阈值**（假 success）与对称房间风险 — **下周**收敛；文档发布。
- 跟进孙昂熙：日志工具 **B3 数据** 异常 — 等应用开发确认；**log merge** 问题跟进。
- 跟进王亚萌：**MK2** 标定/寄送 — 会议说明次日上午可寄出，确认激光侧收货。

## 原始材料

- 工作区逐字稿：`workspace/2026-04-15/inbox/文字记录：04-15 ! 多线组会 2026年4月15日.md`