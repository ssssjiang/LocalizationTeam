# 激光组周报

激光组（多线 SLAM）周度同步与组会纪要的统一存放目录。

## 目录结构

- `laser-weekly-YYYY-W##.md`：周报正文（按 ISO 周编号命名），覆盖一周进展、问题、下周计划。
- `组会纪要/`：每周组会的结构化纪要，按会议日期归档（`YYYY-MM-DD.md`）。

## 维护约定

- 周报与同周组会纪要相互引用：周报「原始材料」段落列出本周组会纪要文件路径。
- 原始逐字稿、飞书智能纪要链接放在 `inbox/` 或对应日期 `workspace/` 目录，本目录只保留结构化产物。
